package com.sarabadani.practice;

import org.junit.Test;

public class BuilderTest {


    @Test
    public void builderTest() {
//        RetryCommand.Builder builder = new RetryCommand.Builder();
//        RetryCommand.Builder preparedBuilder = builder.retryOn(Throwable.class).retryOn(IOException.class).except(IllegalArgumentException.class).run(
//                () -> {
//                    throw new IllegalArgumentException("Illegal error");
//                }
//        ).retryFor(10);
//
//
//        RetryCommand command = preparedBuilder.build();



    }

}